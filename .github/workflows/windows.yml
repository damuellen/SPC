name: CI – Windows

on: [push]

jobs:
  continuous-integration:
    timeout-minutes: 15
    runs-on: [windows-2019]

    steps:

      - name: vcpkg install libxlsxwriter
        run: vcpkg install libxlsxwriter:x64-windows
        continue-on-error: true

      - name: Setup Swift compiler
        uses: compnerd/gha-setup-swift@main
        with:
          branch: swift-5.5.2-release
          tag: 5.5.2-RELEASE

      - name: Checkout sources
        uses: actions/checkout@v2

      - name: Build Release Mode
        run: swift build -c release -Xswiftc -gnone -Xswiftc -suppress-warnings -Xswiftc -continue-building-after-errors -Xswiftc -LC:/vcpkg/installed/x64-windows/lib/
        continue-on-error: true

      - name: Run UPX
        uses: svenstaro/upx-action@v2
        continue-on-error: true
        with:
          file: .build\x86_64-unknown-windows-msvc\release\*.exe
          args: -9
          strip: false

      - name: Upload build artifacts
        uses: actions/upload-artifact@v2
        continue-on-error: true
        with:
          name: SPC-win32-x64
          path: |
            .build\x86_64-unknown-windows-msvc\release\*.exe
            .build\x86_64-unknown-windows-msvc\release\*.dll
